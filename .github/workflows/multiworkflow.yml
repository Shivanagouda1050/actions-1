name: Simple CI/CD Workflow   # 👋 Name shown in GitHub Actions tab

on: push   # ⚡ Runs whenever you push to repo

jobs:
  # -----------------------
  # Job 1: Build
  # -----------------------
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build step
        run: echo "✅ Building the project..."

  # -----------------------
  # Job 2: Test
  # -----------------------
  test:
    runs-on: ubuntu-latest
    needs: build          # This waits until "build" finishes
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test step
        run: echo "🧪 Running tests..."

  # -----------------------
  # Job 3: Deploy
  # -----------------------
  deploy:
    runs-on: ubuntu-latest
    needs: test           # This waits until "test" finishes
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy step
        run: echo "🚀 Deploying application..."
